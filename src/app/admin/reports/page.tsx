import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function AdminReportsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Reportes</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Aquí se generarán y mostrarán los reportes de la clínica.</p>
      </CardContent>
    </Card>
  );
}
